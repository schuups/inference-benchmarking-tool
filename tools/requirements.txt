# Laptop-side tool dependencies — install with: uv pip install -r tools/requirements.txt
pydantic>=2.11
pytest>=8
pyyaml>=6
datasets>=3        # HF-backed dataset sources (brings huggingface_hub + pyarrow)
aiohttp>=3.12      # load generator client + mock server
pytest-asyncio>=1  # async tests (mock server, load gen)
jinja2>=3.1        # planner templates
pandas>=2          # report panels
matplotlib>=3.9    # report plots
nbformat>=5        # report notebook template
nbclient>=0.10     # headless notebook execution
ipykernel>=6       # kernel for nbclient
